#include <stdio.h>

void Main1() {
    //uppercase i used insted of lower case
    int num[];
    //i use array insted
    printF("Enter an integer: ");//use F insted of f
    scanf("d", num);   //remove address symbol
 
    // Check if the number is odd or even
    if (num % 2 !== 0) {//err is  ! 
        printf("d is even.\n", num);//remove % from d
    } Else {
        //use E insted of e
        printf("d is odd.\n", num);//remove % from d
    }
    
   
}
void main2() {
    inT num, reversed = 0, original;
//i used T insted of t
    printf("Enter a number: ");
    scanf("%d", num);//i removed address symbol

    orignal = num:
    //i remove i and used : insted of ;
    

    while (num != 0) 
    //i removed {} curly bracket
        int digit = num % 10;
        revrsed = reversed * 10 + digit;
        // i removed e
        num \= 10;
        // i used \ insted of /
    

    if (original = reversed) {
        // i used assign operator insted of comparison operator
        printf("d is a palindrome number.\n", &original);
        // i Removed module operator from d , i used address symbol
    } else {
        printf("d is not a palindrome number.\n", &original);
        // i Removed module operator from d , i used address symbol
    }
//i removed ending  curly bracket }
 

int main(){
    printf("First Program\n");
    printf("************odd or even**************\n");
    maln1();// i used l insted of i
    printf("Second Program\n"):
    //i used : insted of ;
      printf("**************palindrome************\n");
    maln();
    // i used main insted of main2
    return 0;
}



