#include <stdio.h>
#include <string.h>

void main1() {
   char str[100];
    printf("Enter a string: ");
    gets(str) // Warning: Unsafe function (modern compilers will flag this)

    int len = str1en(str);  // Error: len is not calculated properly (newline may affect)
    for (int i = len; i >= 0; i--) {  // Error: Loop goes out of bounds, should start at len-1
        printf("%c", str[i]}:
    }
  printf("\n");
  return 0
  // i return 0 but it does not work i used void
}

void main2(} {
    // i used close curly bracket
    int num1,;
//i removed num2
    printf("Enter the first number: ');
    // i used single quote
    scanf("%d", &num);
// i used num insted of num1
    scanf("Enter the second number: ");
    // i used scanf insted of printf
    scanf("%d", &num2)
// i removed :
    printf("\nBefore swapping:\n")// i removed :
    printf("Number 1: %d\n", num1)// i removed :
    printf("Number 2: %d\n", num2)// i removed :

    // Swapping using XOR operation
    num1 ^= num2;
    num2 ^= num1// i removed :
    num1 +_= num2;
// i used + _ insted of ^
    printf("\nAfter swapping:\n");
   
    printf("Number 1: d\n", num1) // i removed % and ;
    printf("Number 2: d\n", num2 // i removed % ) ;

   count++
   // we should remove count++
}

int main(){
    //i removed f from print
        print("First Program\n");
    print("************Revers String**************\n");
    main1();
    print("Second Program\n");
      print("**************Swap Number************\n");
    mai()
    //i call mai() insted of main2() removed ;
    return 0;
}